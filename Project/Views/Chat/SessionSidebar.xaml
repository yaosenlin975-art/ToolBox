<UserControl x:Class="ToolBox.Views.Chat.SessionSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ToolBox.Views.Chat"
             Width="220" Background="{StaticResource ChatSidebarBgBrush}">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="14,12" BorderBrush="{StaticResource ChatSidebarBorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Sessions" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                <Button x:Name="NewSessionBtn" Grid.Column="1" Content="+" HorizontalAlignment="Right"
                        Width="30" Height="30" FontSize="18" Padding="0"
                        Style="{StaticResource BtnDefault}" Click="NewSessionBtn_Click" ToolTip="New Session"/>
            </Grid>
        </Border>

        <ListBox x:Name="SessionList" Grid.Row="1"
                 Background="Transparent" BorderThickness="0"
                 SelectionChanged="SessionList_SelectionChanged">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="ItemBd" Padding="0" Margin="2,1" CornerRadius="4"
                                        Background="Transparent" Cursor="Hand"
                                        MouseRightButtonUp="SessionItem_RightClick">
                                    <Grid Margin="10,8,6,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="P" FontSize="10" Margin="0,0,4,0"
                                                           FontFamily="{StaticResource FontSans}" FontWeight="SemiBold"
                                                           Foreground="{StaticResource AccentBrush}"
                                                           Visibility="{Binding IsPinned, Converter={StaticResource BoolToVis}}"/>
                                                <TextBlock Text="{Binding Title}" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding UpdatedAtLocal, StringFormat='{}{0:MM-dd HH:mm}'}" FontSize="11" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Button x:Name="DeleteBtn" Grid.Column="1" Content="X" Width="22" Height="22"
                                                FontSize="10" Padding="0" Opacity="0.7"
                                                VerticalAlignment="Center" Cursor="Hand"
                                                Visibility="Collapsed" Click="DeleteSessionBtn_Click"
                                                Foreground="{StaticResource DangerBrush}"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBd" Property="Background" Value="{StaticResource ChatSidebarHoverBrush}"/>
                                        <Setter TargetName="DeleteBtn" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="ItemBd" Property="Background" Value="{StaticResource ChatSidebarActiveBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>