<Window x:Class="ToolBox.Views.Todo.TodoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ToolBox.Views.Todo"
        Title="TodoList" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" Background="{StaticResource BgSecondaryBrush}">
    <Window.Resources>
        <local:BoolToStrikethroughConverter x:Key="BoolToStrikethrough"/>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="TodoList" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>

        <!-- Add Todo -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewTodoInput" Grid.Column="0"
                     FontSize="13" Padding="8,6" Margin="0,0,8,0"
                     KeyDown="NewTodoInput_KeyDown"/>
            <Button x:Name="AddBtn" Grid.Column="1" Content="添加" Padding="12,6"
                    Style="{StaticResource BtnDefault}" Click="AddBtn_Click"/>
        </Grid>

        <!-- Filter -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,6">
            <RadioButton x:Name="FilterAll" Content="全部" IsChecked="True"
                         Checked="Filter_Changed" Margin="0,0,12,0"/>
            <RadioButton x:Name="FilterPending" Content="待办"
                         Checked="Filter_Changed" Margin="0,0,12,0"/>
            <RadioButton x:Name="FilterCompleted" Content="已完成"
                         Checked="Filter_Changed"/>
        </StackPanel>

        <!-- Todo List -->
        <ListBox x:Name="TodoList" Grid.Row="2" Margin="0,28,0,0"
                 Background="Transparent" BorderThickness="0"
                 SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8,6" Margin="0,1" CornerRadius="4"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="DoneCheck" Grid.Column="0" IsChecked="{Binding IsCompleted}"
                                      VerticalAlignment="Center" Margin="0,0,8,0"
                                      Checked="DoneCheck_Changed" Unchecked="DoneCheck_Changed"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}" FontSize="13"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           TextDecorations="{Binding IsCompleted, Converter={StaticResource BoolToStrikethrough}}"/>
                                <TextBlock Text="{Binding Description}" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
                                <TextBlock FontSize="10" Margin="0,2,0,0"
                                           Foreground="{StaticResource AccentBrush}"
                                           Text="{Binding StatusText, Mode=OneWay}"/>
                            </StackPanel>
                            <Button Grid.Column="2" Content="&#x2715;" FontSize="11" Width="24" Height="24"
                                    Background="Transparent" BorderThickness="0"
                                    Foreground="{StaticResource TextTertiaryBrush}"
                                    Click="DeleteBtn_Click" Tag="{Binding Id}" Cursor="Hand"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>