<UserControl x:Class="ToolBox.Views.Todo.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2009"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="360">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="BtnPrev" Content="◀" Style="{StaticResource BtnDefault}" Margin="0,0,4,0"/>
            <Button x:Name="BtnNext" Content="▶" Style="{StaticResource BtnDefault}" Margin="0,0,4,0"/>
            <Button x:Name="BtnToday" Content="今天" Style="{StaticResource BtnPrimary}" Margin="8,0,8,0"/>
            <TextBlock x:Name="MonthLabel" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
        </StackPanel>

        <!-- Weekday headers -->
        <UniformGrid Grid.Row="1" Columns="7" Margin="0,0,0,4">
            <Border Height="28"><TextBlock Text="日" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/></Border>
            <Border Height="28"><TextBlock Text="一" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/></Border>
            <Border Height="28"><TextBlock Text="二" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/></Border>
            <Border Height="28"><TextBlock Text="三" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/></Border>
            <Border Height="28"><TextBlock Text="四" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/></Border>
            <Border Height="28"><TextBlock Text="五" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/></Border>
            <Border Height="28"><TextBlock Text="六" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/></Border>
        </UniformGrid>

        <!-- Month grid -->
        <UniformGrid Grid.Row="2" Columns="7" x:Name="MonthGrid" Margin="0,4,0,0"/>
    </Grid>
</UserControl>
