<Window x:Class="ToolBox.Views.StyleEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑样式" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="样式名称:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtName" Width="200" Margin="5,0,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ListBox x:Name="lstItems" Grid.Column="0"/>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Button Content="添加" Click="BtnAddItem_Click" Width="80" Margin="0,0,0,5"/>
                <Button Content="删除" Click="BtnRemoveItem_Click" Width="80"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ListBox x:Name="lstKeys" Grid.Column="0"/>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Button Content="添加" Click="BtnAddKey_Click" Width="80" Margin="0,0,0,5"/>
                <Button Content="删除" Click="BtnRemoveKey_Click" Width="80"/>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Click="BtnOk_Click" Width="80" Margin="0,0,5,0"/>
            <Button Content="取消" Click="BtnCancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
