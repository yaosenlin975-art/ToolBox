# 2026-07-13

## 修复 TodoDetailWindow 任务详情弹窗三处问题
文件：`Project/Views/Todo/TodoDetailWindow.xaml` + `.xaml.cs`

1. **进度无法调整** — 原只读 `TextBlock`(ProgressText) 改为 `ModernSlider`(0–100, 步进10) + 百分比显示；`ValueChanged` 实时更新文本并 `UpdateAsync(progress:)` 持久化（沿用 TodoView 的 DetailProgress 模式）。
2. **描述框消失** — 原折叠的只读 `TextBlock`(Description/DescLabel) 改为常驻可编辑 `TextBox`(DescriptionBox)，`LostFocus` 时 `UpdateAsync(description:)` 保存。
3. **废弃按钮位置** — 将 `TrashBtn` 从底部操作栏移到 `添加子任务`(AddSubBtn) 旁边（同一 Horizontal StackPanel）。底部栏(删除/恢复)重命名为 `FooterBorder`，仅当 `IsTrashed` 时可见，避免空白底栏。

验证：`dotnet build` 仅因运行中的 ToolBox.exe 占用 bin 文件导致复制失败(MSB3021)，C# 编译零错误。需关闭运行实例后重新生成。

## 创建 ToolBox 开发专家组（Expert Manager / Team 型）
用 expert-manager skill 创建了项目专属研发专家团 `toolbox-dev-team`（categoryId=02-Engineering），已注册到 marketplace.json，可在「专家中心-我的专家」使用。
- 位置：`~/.workbuddy/plugins/marketplaces/my-experts/plugins/toolbox-dev-team/`
- 成员（主理人+8）：齐活林(交付总监/lead)、许清楚(产品)、高见远(架构)、寇豆码(开发)、苏格调(UI)、严过关(测试)、智言溪(AI/LLM)、文以载(文档)、安部属(发布)。
- 注意：ImageGen **并行生成会文件名冲突互相覆盖**，必须串行逐个生成并立即 rename 到目标名（avatars/ 下 9 成员+team 共 10 张）。
- SOP：需求→架构→设计→开发⇄LLM→测试→文档→发布，主理人汇编。

## ToolBox 全面审查 + 竞品调研（toolbox-audit 团队）
用 toolbox-dev-team 专家组建立 4 人协作团队并行审查，共产出 6 份报告。

**产出文件**：
- `Docs/audit/Architecture-Audit.md`（架构师，50 项问题 14高/22中/14低）
- `Docs/audit/Functionality-Audit.md`（测试，44 项核查 14 未实现 + 17 缺陷 4 P0）
- `Docs/audit/UI-Audit.md`（UI，47 项问题，评分 5/10）
- `Docs/audit/Design-Improvement-Suggestions.md`（产品，22 条改进建议）
- `Docs/Competitor-Research.md`（产品，14 款竞品 + 20 可移植功能）
- `overview.md`（交付总监综合报告）

**最高危发现（P0）**：
1. LLM 文件工具安全防护完全缺失（FileAccessWhitelist/ConfirmDialog 已实现未调用，LLM 可任意删文件，提示词还谎称已防护）
2. 流式输出每 token 全量 Markdown 重解析 + 读磁盘 config.xml
3. 500ms 定时器空转致 ScreenshotTracker 每小时 7200 次文件写入
4. ContextCompressor 整个类 + MemoryStore 读取链路未接入 Agent（只写不读）
5. Anthropic/Ollama Provider 不支持工具调用（仅 OpenAI 支持）
6. DesignTokens 圆角令牌全为 0，LLM-Colors 无暗色，18 处 StaticResource 误用

**未实现功能**：可重复待办（完全未实现）、TodoItem.StartDate、Agent 重试机制、API Key 加密、单轮多工具调用、HistoryView 搜索、OCR 工具（提示词谎称有）

**竞品可移植 P0**：离线 OCR、剪贴板历史、屏幕取色器

**后续路由**：涉及 LLM 的 8 项需求建议走 Workflow C 由 ai-llm-expert 主导。design.md 有 11 条偏离需更新。

## 漏洞修复 + 死代码复活 + 可移植功能设计（toolbox-fix-and-design 团队）
基于审查报告，3 路并行完成。

**修复（13 项 · 编译通过 0 新增错误）**：
- WPF 8 项：FileAccessWhitelist 接入、删除 GC.Collect、WindowManager 条件触发、MessageBubble 流式延迟、ScrapBook 事件转发、托盘菜单映射、数据目录 TODO、XAML UTF-8 BOM
- LLM 5 项：ContextCompressor 接入 Agent、MemoryStore 读写闭环、提示词修正、AnthropicProvider 工具调用、Agent 重试

**设计文档（11 文件 · 20 功能）**：
- `Docs/design/features/`：P0(3个 17人天) + P1(6个 29人天) 详细设计 + P2+P3(11个) 概要设计
- 所有设计对齐 design.md 分层架构/DesignTokens/命名规范
- 交付报告：`Docs/audit/Fix-and-Design-Delivery.md`

下一步：qa-engineer 回归验证修复、design.md 同步更新（6 处）、P0 功能（OCR/剪贴板/取色器）开发启动。

## 回归测试结果（toolbox-fix-test 团队）
测试报告：`Docs/audit/Test-Report.md`
- WPF 8 项：全部通过 ✅
- LLM 4/5 通过 ✅，1 项阻塞 ❌（Agent.cs CS1626/CS1631 yield return in try/catch）
- 阻塞项已修复（List 缓冲区模式），编译通过 0 错误
- **最终结论：14/14 全部通过** ✅

测试还发现 4 个新问题（1 P0 + 2 P2 + 1 P3），详情见 Test-Report.md。

## 可移植功能产品 Spec（product-manager）
产出：`Docs/specs/` 下 10 个文件
- P0+P1：9 个详细 Spec（OCR/剪贴板/取色器/动作链/长截图/智能日期/标签/代码片段/预览卡），每个含 8 章节（概述/用户故事/AC/范围/流程/非功能/依赖/竞品）
- P2+P3：1 个简要 Spec（11 个功能）
- 验收标准可直接转 QA 测试用例，Out of Scope 明确边界防范围蔓延
