# 2026-07-13

## 修复 TodoDetailWindow 任务详情弹窗三处问题
文件：`Project/Views/Todo/TodoDetailWindow.xaml` + `.xaml.cs`

1. **进度无法调整** — 原只读 `TextBlock`(ProgressText) 改为 `ModernSlider`(0–100, 步进10) + 百分比显示；`ValueChanged` 实时更新文本并 `UpdateAsync(progress:)` 持久化（沿用 TodoView 的 DetailProgress 模式）。
2. **描述框消失** — 原折叠的只读 `TextBlock`(Description/DescLabel) 改为常驻可编辑 `TextBox`(DescriptionBox)，`LostFocus` 时 `UpdateAsync(description:)` 保存。
3. **废弃按钮位置** — 将 `TrashBtn` 从底部操作栏移到 `添加子任务`(AddSubBtn) 旁边（同一 Horizontal StackPanel）。底部栏(删除/恢复)重命名为 `FooterBorder`，仅当 `IsTrashed` 时可见，避免空白底栏。

验证：`dotnet build` 仅因运行中的 ToolBox.exe 占用 bin 文件导致复制失败(MSB3021)，C# 编译零错误。需关闭运行实例后重新生成。
